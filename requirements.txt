# ─────────────────────────────────────────────────────────────────────────────
# Smart Parking Lot — Python dependencies
# Compatible with Python 3.10+
# ─────────────────────────────────────────────────────────────────────────────

# ── core framework ────────────────────────────────────────────────────────────
pydantic>=2.0.0,<3.0.0          # Typed models (V2 API)

# ── web UI ────────────────────────────────────────────────────────────────────
gradio>=4.0.0,<5.0.0            # Gradio dashboard (HF Spaces)

# ── baseline inference script ─────────────────────────────────────────────────
openai>=1.0.0,<2.0.0            # GPT-4o-mini inference (optional for baseline)

# ── utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0            # Load .env for local dev (OPENAI_API_KEY etc.)
